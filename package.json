{
  "name": "jeyko-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "npx serve ."
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
