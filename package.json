{
  "name": "jeyko-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "sync": "node scripts/sync-projects.js",
    "build": "node scripts/sync-projects.js && node build.js",
    "dev": "npx serve . -l 987"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
